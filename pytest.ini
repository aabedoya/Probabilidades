[pytest]
minversion = 6.0
addopts = -v --tb=short
testpaths = .
markers =
    slow: marca las pruebas como lentas (usa -m 'not slow' para omitirlas)
    integration: marca las pruebas de integración
python_files = test_*.py *_test.py
python_classes = Test* *Test
python_functions = test_*
